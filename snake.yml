name: Generate Snake Game

on:
  schedule: 
    - cron: "0 */12 * * *"  # Runs every 12 hours
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Generate Snake Game
        uses: Platane/snk@v2
        with:
          github_user_name: Subha8825
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
